"""Anonymized labels for org-facing shortlists (blind IDs live in the repository)."""

from __future__ import annotations

# Identity stripping for API responses is handled by the repository `get_shortlist`
# and `anonymize_*` helpers in the scoring package.

__all__: list[str] = []
